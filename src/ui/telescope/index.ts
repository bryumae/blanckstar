// Telescope screen (mvp0_spec.md §7.1): outside view + telescope mode,
// click-to-identify, angular separation measurement. Not implemented yet —
// scaffolding only.
export function mountTelescopeScreen(root: HTMLElement): void {
  root.textContent = 'Telescope';
}
