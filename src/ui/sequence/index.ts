// Sequence & Calculation screen (mvp0_spec.md §7.6, §7.7, §7.9): script
// console, calculator, candidate estimates/search tables, trajectory
// predictor. Not implemented yet — scaffolding only.
export function mountSequenceScreen(root: HTMLElement): void {
  root.textContent = 'Sequence & Calculation';
}
